# Sprint Status
# Project: crm (Tiberbu CRM fork)
# Tracking: Claude Code Studio
# Updated: 2026-08-26

summary:
  total: 50
  ready-for-dev: 21
  in-progress: 2
  review: 19
  done: 3
  cancelled: 0

development_status:

  # Chain: epic-ses-inbound
  epic-ses-inbound: in-progress

  # E1 — AWS Provisioning (done — code shipped in careverse_fixes PR #4)
  ses-inbound-e1-s1-1-s3-bucket-lifecycle: done           # [dev-story] S3 bucket + 48h lifecycle + SES bucket policy
  ses-inbound-e1-s1-2-sns-topic-subscription: done        # [dev-story] SNS topic + HTTPS subscription
  ses-inbound-e1-s1-3-ses-receipt-rule: done              # [dev-story] SES receipt rule set + rule

  # E2 — CRM Webhook (done — code shipped in careverse_fixes PR #4)
  ses-inbound-e2-s2-1-sns-verify-confirm: done            # [dev-story] SNS sig verify + subscription confirm
  ses-inbound-e2-s2-2-mime-fetch-branch: done             # [dev-story] Inline vs S3 MIME fetch branch
  ses-inbound-e2-s2-3-thread-matching: done               # [dev-story] Thread match → Communication
  ses-inbound-e2-s2-4-attachment-storage: done            # [dev-story] Attachment → Frappe File

  # E3 — Settings UI (done — code shipped in careverse_fixes PR #4)
  ses-inbound-e3-s3-1-doctype-fields: done                # [dev-story] DocType + api/ses.py: 4 inbound AWS fields
  ses-inbound-e3-s3-2-vue-provision-ui: done              # [dev-story] SESSettings.vue: Inbound section + provision button

  # Pending: DevOps execution + end-to-end test
  ses-inbound-devops-provision: ready-for-dev             # [task] DevOps: run provision(), add MX record, confirm SNS
  ses-inbound-qa-e2e: ready-for-dev                       # [qa] E2E: send test email → Communication + File created

  # Chain: epic-hfr-integration (v1.1 — flat-field model, SUPERSEDED by cardinality refactor)
  epic-hfr-integration: superseded                # cardinality was wrong; refactor in epic-hfr-refactor

  # v1.1 stories — shipped code exists but will be replaced by refactor
  hfr-p1-doctype-org-fields: superseded           # flat columns → child table in refactor
  hfr-p1-doctype-lead-fields: superseded          # flat columns → child table in refactor
  hfr-p1-settings-fields: done                    # CRM HFR Settings — no change needed
  hfr-p1-api-module: superseded                   # resync_organization replaced by resync_facility_row
  hfr-p2-composable: superseded                   # applyHfrPreview replaced by append-row semantics
  hfr-p2-org-modal: superseded                    # modal Facilities section redesigned
  hfr-p2-lead-modal: superseded                   # modal Facilities section redesigned
  hfr-p3-org-detail-registry: superseded          # flat Registry tab → child table Facilities tab
  hfr-p4-settings-ui: done                        # HFRSettings.vue — no change needed
  hfr-qa-full-flow: superseded                    # re-run after refactor

  # Chain: epic-hfr-refactor (cardinality fix — BRD + ADR complete)
  epic-hfr-refactor: ready-for-dev

  # Phase 1 — Child DocTypes + migration
  hfr-c1a-crm-org-facility-doctype: review       # [dev-story] CRM Org Facility child DocType
  hfr-c1b-crm-lead-facility-doctype: review      # [dev-story] CRM Lead Facility child DocType
  hfr-c1c-crm-deal-facility-doctype: review      # [dev-story] CRM Deal Facility child DocType
  hfr-c1d-migration-patch: review                # [dev-story] patch: flat→child rows + drop columns
  hfr-c1e-parent-doctype-updates: review         # [dev-story] Org/Lead/Deal JSON: remove flat, add table

  # Phase 2 — Python backend
  # hfr-c2a removed — no validate() roll-up (OQ-1 resolved: row-level sync only)
  hfr-c2b-lead-py-refactor: review               # [dev-story] crm_lead.py: create_organization() uses child rows
  hfr-c2c-api-resync-row: review                 # [dev-story] hfr.py: resync_facility_row(), remove flat resync
  hfr-c2d-convert-to-deal: review                # [dev-story] convert_to_deal() copies facility rows

  # Phase 3 — Frontend: modals
  hfr-c3a-search-panel-append: review            # [dev-story] HfrSearchPanel.vue: append-row + pending list
  hfr-c3b-org-modal-facilities: review           # [dev-story] OrganizationModal.vue: Facilities section
  hfr-c3c-lead-modal-facilities: review          # [dev-story] LeadModal.vue: Facilities section

  # Phase 4 — Frontend: detail pages
  hfr-c4a-org-facilities-tab: review             # [dev-story] Organization.vue: Facilities tab
  hfr-c4b-lead-facilities-section: review        # [dev-story] Lead.vue: Facilities section
  hfr-c4c-deal-facilities-section: review        # [dev-story] Deal.vue: Facilities section

  # Phase 5 — QA
  hfr-c5-qa-refactor: review                     # [qa] Migration + multi-facility + convert-to-deal + re-sync

  # Chain: epic-finance-cockpit (BRD + ADR confirmed 2026-08-06)
  epic-finance-cockpit: in-progress

  # Sprint 1 — Foundation
  fc-s1-1-crm-partner-doctype: review               # [dev-story] CRM Partner + CRM Rebate Structure DocTypes; partner field on CRM Lead
  fc-s1-2-lead-approval-workflow: review             # [dev-story] Frappe Workflow on CRM Lead: Draft→Pending→Approved/Rejected; Partner RM vs Sales Manager routing
  fc-s1-3-cockpit-page-shell: review                # [dev-story] Frappe Page registration; Vue sidebar + topbar + company switcher + mobile hamburger
  fc-s1-4-kpi-strip: review                         # [dev-story] get_finance_kpis API; KPI tiles + period selector; role-scoped + company-aware

  # Sprint 2 — Inbox + AR/AP worklists + Onboarding Request
  fc-s2-1-pending-actions-inbox: review              # [dev-story] get_pending_actions API; Inbox UI with 13 item types; urgency bands; role-scoped
  fc-s2-2-receivables-section: review                # [dev-story] AR Invoices + Sales Orders + Payments + Customers; FinanceTable + StatusPill components
  fc-s2-3-payables-section: review                   # [dev-story] AP Invoices + Purchase Orders + Supplier Payments + Suppliers; approve_purchase_invoice
  fc-s2-4-onboarding-request-doctype: review         # [dev-story] CRM Onboarding Request DocType; auto-create on Deal Won; provisioning_complete endpoint

  # Sprint 3 — Expenses, Banking, Partner & Commission
  fc-s3-1-rebate-commission-doctypes: review        # [dev-story] CRM Partner Rebate Voucher + CRM Sales Commission DocTypes; Payment Entry on_submit hook
  fc-s3-2-expenses-section: review                  # [dev-story] Expense Claims + Employee Advances + Expense Journals; hrms_adapter.py
  fc-s3-3-partner-commission-section: review        # [dev-story] Rebate Vouchers + Sales Commissions tabs; approve/confirm/mark-paid APIs; role-scoped
  fc-s3-4-banking-section: review                   # [dev-story] Bank Accounts + Bank Transactions; reconciliation link

  # Sprint 4 — Dashboard charts, Reports, GL/Assets/Liabilities
  fc-s4-1-dashboard-charts: review                  # [dev-story] Cashflow + AR Aging + AP Aging + P&L charts; get_dashboard_charts API; lazy-loaded
  fc-s4-2-reports-panel: review                     # [dev-story] 32 report links in 6 groups; reportsConfig.js; role-scoped; pre-filtered URLs
  fc-s4-3-gl-liabilities-assets: review             # [dev-story] GL viewer + Journal Entries + Period Closing; Subscriptions + Deferred; Asset Register

  # Sprint 5 — Scoped views, Global search, Setup, erpnext_adapter
  fc-s5-1-scoped-views: review                      # [dev-story] SalesManagerView + PartnerRmView; restricted sidebar; read-only commission/rebate lists
  fc-s5-2-global-search: review                     # [dev-story] Ctrl+K command palette; global_search API; role-scoped results; keyboard navigation
  fc-s5-3-setup-erpnext-adapter: review             # [dev-story] Setup section links (Finance Manager only); erpnext_adapter.py cross-site wrapper

  # Sprint 6 — Create-From (mapped-doc) flows
  fc-s6-1-create-from-mapped-docs: review           # [dev-story] Orders "Create from Quote" + Invoices "Create from Quote"/"Create from Order" via native ERPNext mappers; inline submitted-source picker (company+docstatus=1, name+party search); FinanceForm seed path

  # QA — full cockpit integration pass (all 18 stories complete)
  fc-qa-full-cockpit: qa                 # [qa] Full Finance Cockpit end-to-end QA pass: all roles, all sections, mobile, dark/light mode

  # Chain: epic-partner-portal
  epic-partner-portal: ready-for-dev

  partner-portal-e1-api-leads: ready-for-dev              # [dev-story] Partner API: create/get/list/update leads
  partner-portal-e1-api-deals: ready-for-dev              # [dev-story] Partner API: create/get/list/update deals
  partner-portal-e1-api-contacts: ready-for-dev           # [dev-story] Partner API: create/list/link contacts
  partner-portal-e2-webhooks: ready-for-dev               # [dev-story] Outbound webhooks: lead/deal lifecycle events
  partner-portal-e3-ref-data: ready-for-dev               # [dev-story] Reference data endpoints + rate limiting
  partner-portal-qa: ready-for-dev                        # [qa] Partner portal full flow QA

  # Chain: epic-crm-quote-builder (BRD + ADR + PRD confirmed 2026-08-07)
  epic-crm-quote-builder: in-progress

  # Sprint 1 — Backend Foundation
  qb-s1-1-crm-quote-doctypes: review                # [dev-story] CRM Quote + Facility + Add-on + Renewal DocTypes; 15 product catalogue SKUs; Sales Invoice custom fields patch
  qb-s1-2-quote-api: review                         # [dev-story] crm/api/quotes.py: all API methods, pricing engine, RBAC scoping; invoice_adapter.py
  qb-s1-3-print-format: review                      # [dev-story] CRM Quote Standard print format: complete, test, migrate; PDF download verified
  qb-s1-4-remove-legacy-redirect: review             # [dev-story] Remove doctype_js Quotation hook; deprecate erpnext_quotation_prefill.js

  # Sprint 2 — Quote Builder UI
  qb-s2-1-quoting-tab-and-list: review              # [dev-story] Quoting tab on Deal.vue; QuotingTab.vue; /crm/quotes global list view; Finance Cockpit handoff banner
  qb-s2-2-builder-step1-facilities: review          # [dev-story] QuoteBuilder.vue overlay shell; Step 1 facility cards with tier pills + live subtotal
  qb-s2-3-builder-steps2-3: review                  # [dev-story] Step 2 add-ons table; Step 3 discount sliders + payment terms + live pricing + 5-yr TCO
  qb-s2-4-builder-step4-actions: review             # [dev-story] Step 4 review preview; Send/Accept/Reject wiring; PDF download

  # Sprint 3 — Integration + QA
  qb-s3-1-erpnext-handoff: review                   # [dev-story] accept_quote → Sales Invoice → Finance Cockpit AR; crm_deal + crm_quote fields verified
  qb-s3-2-qa-full-pass: qa               # [qa] 19/20 pass; T-UI-12 mobile fix applied (MobileDeal.vue Quoting tab); rebuild clean

  # Chain: epic-optin-portal (BRD-OPTIN-001 · ADR-003 — 24 Aug 2026)
  epic-optin-portal: in-progress

  # Sprint 1 — Prerequisites (DocTypes + RBAC + price lists)
  optin-s0-1: review        # [dev-story] DocTypes: CRM Opt-In Network + CRM Pre-Qualified Facility + CRM Opt-In Submission; bench migrate ✓ all 3 tables present
  optin-s0-2: review        # [dev-story] Network Coordinator role + assigned_network User field + permission_query_conditions; bench migrate ✓ role+field exist, hooks.py wired
  optin-s0-3: review        # [dev-story] Patch: 7 KEPH Items × 5 Negotiated Price Lists = 35 Item Price records; idempotent; bench migrate ✓ count=7

  # Sprint 2 — API layer
  optin-s1-1: review         # [dev-story] crm/api/optin.py: get_settings, verify_prequalified, verify_otp, get_pricing, get_terms_text, submit_async, get_job_status, save_partial, resume; _process_submission background job

  # Sprint 3 — Vue wizard
  optin-s2-1: review         # [dev-story] Vue wizard 8 steps + Frappe Web Page /opt-in shell; OTP gate, facility cards, pricing, T&C scroll-lock, commit, progress ticks, success screen

  # Sprint 4 — UX + delivery polish (careverse_fixes 26 Aug 2026)
  optin-s3-1: review         # [dev-story] Branded post-submission confirmation email: network logo/colour, reference badge, facilities-registered table, annual+VAT pricing summary, "what happens next" steps, help/footer — replaces plain <p> email. now=True. Proven: _confirmation_email_html renders ref+VAT total(167,040=144k*1.16)+brand+facility
  optin-s3-2: review         # [dev-story] now=True on ALL opt-in/contract/quote sendmail (optin OTP+resume+confirmation, contracts invite+approver+OTP, quotes send+expiry) to bypass Email Queue flush delay — matches proven event.py/crm_invitation.py pattern
  optin-s3-3: review         # [dev-story] StepLanding.vue: expand 3 value-prop pills into explained feature cards (icon+title+one-line) so user understands what they're starting; brand-tinted icons; dark/light parity
  optin-s3-4: review         # [dev-story] CRM Opt-In Partner child DocType → partner_logos Table on CRM Opt-In Network; get_settings exposes absolute logo URLs; "Trusted by our partners" strip on wizard landing. Proven: migrate ✓ child table in DB, get_settings(demo-network) returns 2 logos
  optin-s3-5: review         # [dev-story] Lock already-quoted facilities on the wizard: _get_quoted_facility_map correlates pre-qualified mfl_code → CRM Deal Facility → deals with a CRM Quote; verify_otp emits already_quoted+quoted_deal; get_pricing skips locked codes (server-side guard); StepFacilities.vue non-interactive locked cards (lock icon, "Already quoted on deal X" pill, excluded from Select-All), explanatory banner

  # Chain: epic-contract-signing (BRD-CONTRACT-001 · ADR-004 — 24 Aug 2026)
  epic-contract-signing: ready-for-dev

  # cs-s1-1 already done — CRM Contract + CRM Contract Signatory DocTypes migrated
  cs-s1-1-contract-doctypes: done                  # [dev-story] CRM Contract + CRM Contract Signatory; 3-approver fields on CRM Onboarding Request; migrated

  # Sprint 1 — Contract API
  cs-s1-2-contracts-api: ready-for-dev              # [dev-story] crm/api/contracts.py: generate, request_otp, verify_otp, get_contract, sign, download_pdf; _transition() state machine
  cs-s1-3-hooks-patches: ready-for-dev              # [dev-story] hooks.py doc_events for CRM Contract; after_migrate ensure_signing_key; patches.txt
  cs-s1-4-pdf-print-format: ready-for-dev           # [dev-story] crm_contract_standard print format: contract body + 2 signature blocks; wkhtmltopdf
  cs-s1-5-onboarding-approval-api: ready-for-dev    # [dev-story] crm/api/onboarding.py: approve_onboarding(slot, reject); all-3-approved → Approved; any-reject → Rejected

  # Sprint 2 — Signing portal
  cs-s2-1-signing-portal: review                    # [dev-story] Frappe Web Page /sign-contract; OtpGate reuse; SignatureCanvas (signature_pad.js); scroll-lock before canvas

  # Chain: epic-optin-handoff (gap analysis 25 Aug 2026 — exec-handoff-stories.md; maps BRD-OPTIN-001 Phase 2 + BRD-CONTRACT-001 Phases 4-5)
  epic-optin-handoff: ready-for-dev

  # Sprint 1 — Traceability spine (backend)
  oh-s1-1-provenance-fields: review                 # [dev-story] Patch: Lead opt-in/T&C custom fields + Deal.optin_submission backlink; stop silent .set() drop — proven OIS-2026-00005: 6 Lead CF + Deal.optin_submission in meta, hash persisted, backlink==OIS, quote crm_sent=1
  oh-s1-2-deal-lifecycle-api: review                # [dev-story] crm/api/lifecycle.py get_deal_lifecycle(deal): reverse-resolve OIS+quote+contract+signatories+onboarding+invoice — proven deal 18952 all 6 keys, bare deal all-None never-raise, non-priv→PermissionError, HTTP 200; AC-04 no db.sql/get_all
  oh-s1-3-transition-timeline: review               # [dev-story] log_deal_event() writes comment_type=Comment on 3 transitions (OIS processed/contract generated/both-signed); track_changes=1 on CRM Onboarding Request — proven: migrate exit 0 + get_meta.track_changes==1, Version captured on save (ONB-2026-00004 0→1), deal 97111 Activity shows 3 distinct entries (screenshot /tmp/ohs13-ac02-deal-97111.png), AC-03 no f-strings. NOTE: comment_type=Comment (not Info per spec) — Salim-approved, since Info routes to docinfo.info_logs which get_deal_activities ignores

  # Sprint 2 — Exec pick-up surface (frontend)
  oh-s2-1-submissions-queue: ready-for-dev          # [dev-story] /opt-in-submissions full-page list + sidebar; list_submissions + retry_submission APIs; status filter, deal link, retry-failed
  oh-s2-2-deal-contracting-panel: review     # [dev-story] v3 clean rewrite: QuotingTab.vue — prominent hero card (OIS ref mono, OTP Verified chip, name xl bold, role·org, email link, phone, Email Applicant btn) + facilities table + smart button Cases A/B/C (Build Quote from OIS solid btn when no quote, View/Adjust when quote exists, New Quote only on non-OIS); lifecycle fetched in QuotingTab, passed as prop to ContractingPanel; oisDoc passed as prop. ContractingPanel.vue — no internal OIS/lifecycle fetch; props: dealId+oisDoc+lifecycle; emit lifecycle-reload after generate; pre-fill from explicit facility_signatory fields > raw_json fallback; Exec Notes + Generate Contract form (7 fields, Combobox approvers) + Lifecycle strip. build clean (39s), zero hex (grep empty), Combobox not Autocomplete; screenshots /tmp/ohs22v3-hero-light.png /tmp/ohs22v3-hero-dark.png /tmp/ohs22v3-build-quote.png /tmp/ohs22v3-form.png — deal 48872 OIS-2026-00008 Thomas Mwogi visible, Build Quote from OIS btn present, signatory pre-filled

  # Sprint 3 — Completion chain (backend)
  oh-s3-1-contract-state-machine: ready-for-dev     # [dev-story] both-signed→Awaiting Internal Approval + Deal Won (D-2); all-3-approved→Fully Executed + finalize_contract (idempotent)
  oh-s3-2-finalize-quote-pdf-email: ready-for-dev   # [dev-story] finalize_contract: _finalize_quotation submits Quotation+Sales Invoice (D-1) + executed PDF via CRM Contract Standard + email all parties incl deal contact
  oh-s3-3-reconciliation-scheduler: ready-for-dev   # [dev-story] scheduler: re-enqueue stuck Pending/Processing OIS; create missing invoice for Accepted quotes; idempotent

  # Sprint 4 — Finance posting visibility
  oh-s4-1-finance-ar-provenance: ready-for-dev      # [dev-story] AR crm_deal/crm_quotation columns + fix RBAC-blocked exec deep link (invoices auto-submitted per D-1, no draft view)

  # Chain: epic-optin-contact-us (planned 26 Aug 2026 — "no fallback" directive; BRD pending)
  # Directive: an un-onboarded network must NOT fall through to default CareverseHIMS
  # branding. Instead the visitor is told the network isn't onboarded and routed to
  # their network coordinator + a Contact Us form. Requests land in the CRM for agents
  # to confirm and promote into the Pre-Qualified list / Opt-In Network.
  epic-optin-contact-us: ready-for-dev

  cu-s1-1-contact-request-doctype: ready-for-dev    # [dev-story] CRM Contact Request DocType (facility_name, contact_name, email, phone, network_name free-text, message, status New/In Review/Added/Closed, linked_prequalified_facility, linked_network, handled_by); CRM list view + sidebar entry for agent follow-up
  cu-s1-2-guest-api: ready-for-dev                  # [dev-story] crm/api/contact_request.py submit_contact_request (allow_guest, per-IP rate limit, branded acknowledgement email now=True); no PII leak
  cu-s1-3-no-fallback-wizard: ready-for-dev         # [dev-story] Remove default-branding fallback: get_settings returns matched flag; unknown/disabled slug → wizard shows "Network not onboarded" state → coordinator note + Contact Us form (no CareverseHIMS default path)
  cu-s1-4-crm-followup-actions: ready-for-dev       # [dev-story] Agent actions on CRM Contact Request: "Add to Pre-Qualified Facility" + "Add to Opt-In Network"; status transitions; audit trail

  # Sprint 2 — Seamless CRM destination surfaces (context added 26 Aug 2026)
  # RATIONALE: CRM Opt-In Network + CRM Pre-Qualified Facility currently have NO frappe-ui
  # views (grep frontend/src = 0 hits; no router.js routes) — they are Desk-only today
  # (/app/crm-opt-in-network, /app/crm-pre-qualified-facility). The two are relationally
  # linked (CRM Pre-Qualified Facility.network -> CRM Opt-In Network, reqd). cu-s1-4's
  # "promote" actions would otherwise dump agents into raw Desk forms, breaking the
  # frappe-ui UX everywhere else in the CRM. These stories give the promote-destinations
  # a native home so the follow-up flow is seamless end-to-end.
  cu-s2-1-network-crm-views: ready-for-dev          # [dev-story] frappe-ui list + detail for CRM Opt-In Network: sidebar entry, /crm/opt-in-networks route, status/branding/partner-logos fields, count of linked pre-qualified facilities, RBAC (Network Coordinator scoped to assigned_network)
  cu-s2-2-prequalified-crm-views: ready-for-dev     # [dev-story] frappe-ui list + detail for CRM Pre-Qualified Facility: /crm/pre-qualified route, filter by network, status (Active/Opted In/Declined), breadcrumb link up to parent Opt-In Network, "already quoted" indicator reusing _get_quoted_facility_map; deep-link from Contact Request promote action
  cu-qa: ready-for-dev                              # [qa] Full contact-us flow: submit as guest → CRM record → agent promotes to Pre-Qualified → opt-in succeeds; un-onboarded slug shows no fallback; promote lands on native frappe-ui Network/Pre-Qualified views (not Desk)
